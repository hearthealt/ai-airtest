# -*- encoding=utf8 -*-
"""qwen3-vl-plus视觉语言模型的提示词模板。"""


# ==================== 公共弹窗识别提示词 ====================

POPUP_DETECTION_PROMPT = """## 弹窗识别与处理

### ★★★ 核心原则：区分"登录弹窗"和"登录内容页"
- 弹窗/广告/浮窗**遮挡了**底部导航栏或顶部标签栏 → 必须处理，设 has_popup=true
- **全屏登录页**（**完全没有底部导航栏**，整个屏幕都是登录表单） → 设 has_popup=true, popup_type="login"
- **登录弹窗/浮层**（有半透明遮罩蒙层，登录表单浮在页面上方，底部导航被遮罩盖住无法点击） → 设 has_popup=true, popup_type="login"
- **Tab内登录提示页**（底部导航栏清晰可见且**无遮罩蒙层覆盖**，页面中间显示"登录即可体验完整功能"等提示文字） → **不是弹窗！** 设 has_popup=false，正常识别底部导航栏
- 页面底部/顶部的小广告条、角落的悬浮广告图标 → 全部忽略，不算弹窗

### ★★★ 区分登录弹窗 vs 登录内容页的判断方法：
1. 看截图中底部导航栏区域**有没有被半透明黑色/灰色遮罩覆盖**
2. 如果底部导航栏**被遮罩盖住**（颜色变暗、模糊） → 是登录弹窗，has_popup=true
3. 如果底部导航栏**颜色正常、清晰可见、无遮罩** → 不是弹窗，has_popup=false，正常识别L1导航

### 什么算弹窗（必须处理，has_popup=true）：
1. **隐私政策/用户协议弹窗**：大面积遮罩，必须同意才能继续 → 点"同意"/"同意并继续"
2. **系统权限弹窗**：系统级弹窗遮挡全屏 → 点"允许"/"始终允许"（不要点拒绝！）
3. **大面积广告/引导/升级弹窗**：有遮罩蒙层，挡住了导航控件 → 点关闭(X)/跳过/以后再说
4. **登录弹窗/浮层**：覆盖在主页面上的登录页，挡住了底部导航 → 点关闭按钮
5. **全屏登录页面**：整个页面就是登录页，没有底部导航栏，页面有手机号/密码输入框、登录按钮、第三方登录图标等 → popup_type="login"，popup_close_button返回登录按钮的坐标
6. **青少年模式弹窗**：遮罩弹窗 → 点"我知道了"/"关闭"
7. **操作引导/提示浮层**：遮挡了导航控件导致无法操作 → 点击消除
8. **新用户引导/欢迎页（onboarding）**：首次启动的欢迎页面、性别/年龄/兴趣选择、个性化推荐设置等。处理规则：
   - 优先找"跳过"按钮，有就直接返回"跳过"
   - **没有"跳过"时，必须一步一步来，每次只返回一个操作**：
     - 如果页面要求先选择（性别/年龄/兴趣标签等）且还没有任何选项被选中 → 返回一个选项的坐标（随便选一个即可），**绝对不能返回"下一步/确定"按钮**
     - 如果已经有选项被选中（高亮/勾选状态），再返回"下一步/确定"按钮
   - **关键：如果看到"下一步"按钮但页面上有必选项（性别/年龄等）尚未选择，不要返回"下一步"！先选一个选项！**

### 不算弹窗（忽略不管，has_popup=false）：
- 页面底部的小广告横幅（不挡底部导航栏）
- 角落的悬浮广告小图标/按钮
- 页面内嵌的推荐卡片/横幅
- 顶部的通知条/提示条（不挡顶部Tab栏）
- 不影响点击底部Tab和顶部Tab的任何元素
- **底部导航栏清晰可见且无遮罩覆盖时，页面中间的登录提示（如"登录即可体验完整功能"、"手机号登录/注册"）→ 这是Tab内容页，不是弹窗**

### 重要：popup_close_button 应该返回"需要点击的按钮"：
- 隐私协议 → 返回"同意"按钮（不是拒绝）
- 系统权限 → 返回"允许"/"始终允许"按钮（不要返回"拒绝"！）
- 广告/引导 → 返回关闭(X)/跳过按钮
- 登录弹窗/登录页面 → 返回页面上的关闭/返回按钮（<、←、×、X等）的坐标，text填写按钮的文字或符号（如"<"、"返回"、"×"）
- 青少年模式 → 返回"我知道了"按钮
- 操作引导浮层 → 返回提示文字所在坐标（点击即可消除）
- 新用户引导/欢迎页（onboarding）→ 处理规则：
  1. 优先返回"跳过"按钮
  2. **没有"跳过"时，看页面上是否有必选项（性别/年龄/兴趣标签等）：**
     - **有必选项且未选中 → 返回一个未选中选项的坐标（如随意选一个性别/年龄），不要返回"下一步"！**
     - **选项已选中（有高亮/勾选） → 返回"下一步/确定"按钮**
  3. 每次只返回一个操作，系统会循环调用直到引导结束

### ★★★ 坐标精度要求（极其重要）：
- popup_close_button 的 coordinates 必须是按钮在截图中的**精确位置**，不要返回(0.5, 0.5)等猜测值！
- 仔细看截图中 × / 关闭 / 同意 等按钮的**实际像素位置**，转换为归一化坐标(0.0-1.0)
- 关闭按钮(×)通常在弹窗的**右上角**，坐标大约在 x=0.7~0.9, y=0.2~0.5 的范围
- 如果UI树中有对应元素，优先使用UI树中的pos坐标（最准确）
- 如果UI树中找不到（按钮可能没有text/desc），则从截图中目测按钮中心位置

### 如何区分弹窗和页面：
- **弹窗特征**：有半透明遮罩/蒙层背景（底部导航栏区域颜色变暗/模糊）、有明显的关闭按钮(X/×/<)、登录表单浮在页面上层
- **引导浮层特征**：半透明文字浮在内容上方、无关闭按钮但点击可消除
- **全屏登录页特征**：占满全屏、完全没有底部导航栏、有登录表单 → 视为弹窗，popup_type="login"
- **Tab内登录提示页特征**：底部导航栏清晰可见且颜色正常无遮罩、页面中间有登录提示文字/按钮 → **不是弹窗**，正常识别底部导航
- **正常页面特征**：有底部导航栏、有顶部标签栏、无遮罩背景
- **关键判断**：看底部导航栏区域——被遮罩盖住变暗=弹窗，颜色正常清晰=不是弹窗

### 响应要求：
- 发现遮挡导航的弹窗/浮层时：设 has_popup=true，popup_close_button 返回需要点击的按钮坐标和文字，popup_type 标注类型
- 没有遮挡导航的弹窗，或只有不影响操作的小广告时：设 has_popup=false，popup_close_button 设为 null
- popup_type 取值：
  - "login"：登录弹窗/浮层（有登录按钮和关闭按钮）
  - "permission"：系统权限弹窗
  - "agreement"：隐私政策/用户协议弹窗
  - "ad"：广告/引导/升级弹窗
  - "guide"：操作引导/提示浮层（如"双指撑开可清屏"）
  - "onboarding"：新用户引导/欢迎页（性别/年龄/兴趣选择等）
  - "other"：其他类型弹窗
- 弹窗不算页面本身的内容，忽略弹窗看底下的页面"""


def get_system_prompt() -> str:
    """获取系统提示词"""
    return """你是一个移动应用QA测试员，正在执行阻断测试。

## 你的任务
逐个点击界面上的导航控件，检查点击后的页面是否被成功阻断（无法正常加载数据）。

## 测试层级（只测一级和二级）
- 一级导航：底部导航栏（如：首页、动态、消息、我的等）→ 逐个切换
- 二级导航：每个一级页面内的顶部标签/Tab（如：直播、推荐、热门、追番等）→ 逐个点击检查阻断
- 三级及以下（页面中的视频卡片、列表项、文章、图片等具体内容）→ 不要点击，不需要测试

## 工作流程
1. 如果看到弹窗，优先关闭
2. 先测当前一级页面下的所有二级标签（顶部Tab），每个都点一下检查阻断
3. 当前一级页面的二级标签全部测完后，切换到下一个底部导航（一级），继续测它的二级标签
4. 不要点击页面中间的内容项（视频、图片、文章、卡片等），只测导航类控件

## 页面检查标准

核心判断原则：页面有没有加载出真实的业务数据内容？

### 阻断成功（is_error_screen=true）：页面没有加载出真实业务数据
以下情况都算阻断成功：
- 页面显示"网络错误"、"加载失败"、"连接超时"、"无法连接"等明确错误文字
- 页面显示空列表配合"暂无数据"、"没有内容"等明确提示文字
- 页面崩溃或异常退出
- **页面显示"未登录"、"请先登录"、"登录后查看"等登录提示，只有登录按钮没有真实数据内容 → 算阻断成功（未登录态无法加载数据=阻断生效）**
- **页面只有静态提示文字（如引导文案、空状态图标），没有任何真实业务数据列表/图片/视频 → 算阻断成功**

### 还在加载中，不确定（is_loading=true, is_error_screen=false）：
- 图片区域显示灰色占位图、灰色方块、默认图标（如灰色TV图标、灰色头像）而不是真实图片
- 视频/直播封面是灰色方块或默认占位图，而不是真实的缩略图/封面
- 虽然有文字标题，但图片都是占位符，说明数据没有真正加载完成
- 页面有加载动画（转圈/进度条），内容还没出来
- 关键判断：看图片！文字可能是缓存的，但如果图片全是灰色/占位符，就是没加载出来

### 阻断失败（is_error_screen=false, is_loading=false）：页面加载出了真实的业务数据
- 页面加载出了真实的图片内容（真实视频封面、真实直播画面、真实头像等，不是灰色占位图）
- 列表中有真实的缩略图和内容
- 视频/图片/文章等内容真实显示，不是占位符
- 注意：只有真实业务数据加载出来才算失败，静态页面/提示页/未登录页不算失败

## 响应格式
只返回JSON，不要markdown标记：

{
  "screen_description": "当前页面的简短描述",
  "is_error_screen": true,
  "is_loading": false,
  "error_description": "阻断成功的具体表现（如：页面显示网络错误）/ 或 阻断失败的证据（如：列表数据正常加载）/ 或 加载中的表现（如：灰色占位图）",
  "elements": [
    {
      "name": "元素标识符",
      "text": "元素上的文字",
      "type": "控件类型",
      "control_type": "button|text_field|image|list_item|tab|menu|checkbox|switch|link|dialog|unknown",
      "bounds": {"x": 0.1, "y": 0.2, "width": 0.3, "height": 0.1},
      "center": [0.25, 0.25],
      "clickable": true,
      "enabled": true,
      "visible": true,
      "poco_path": "poco路径（如有）"
    }
  ],
  "actions": [
    {
      "action": "click|back|scroll_down|scroll_up",
      "element_index": 0,
      "coordinates": [0.25, 0.25],
      "is_popup": false,
      "priority": 1,
      "reasoning": "操作理由",
      "confidence": 0.9
    }
  ]
}

## 规则
- 坐标使用归一化值0.0-1.0
- 弹窗处理：系统权限弹窗点"允许/始终允许"，广告/引导弹窗点关闭或跳过
- 点击弹窗按钮时 is_popup=true，点击导航控件时 is_popup=false
- 优先点击还没测试过的菜单/控件
- 返回所有可点击的导航控件作为候选（actions数组可包含多个），按优先级排序，未测过的排前面"""


def get_user_prompt(
    ui_tree_text: str,
    exploration_context: str,
    explored_elements: list,
) -> str:
    """
    构建用户提示词。

    :param ui_tree_text: 格式化后的UI树文本
    :param exploration_context: 探索上下文信息
    :param explored_elements: 已探索元素名称列表
    :return: 完整的用户提示词
    """
    explored_str = "\n".join(f"  - {e}" for e in explored_elements[-30:]) if explored_elements else "  (暂无)"

    return f"""分析截图中的当前页面。

## UI层级结构
{ui_tree_text}

## 当前状态
{exploration_context}

## 已经点击测试过的控件（不要重复点击）
{explored_str}

请分析这个页面，返回JSON结果。"""


# ==================== 结构化L1→L2导航：专用发现提示词 ====================


def get_discover_l1_system_prompt() -> str:
    """L1发现：识别底部导航栏的所有一级菜单项"""
    return """你是一个移动应用UI分析员。你的任务是识别当前界面**底部导航栏**的所有一级菜单项。

## 任务
找出屏幕底部导航栏的所有一级菜单项。底部导航栏通常是几个图标+文字的组合，固定在屏幕最底部。

""" + POPUP_DETECTION_PROMPT + """

## 响应格式
只返回JSON，不要markdown标记：

{
  "has_popup": false,
  "popup_type": "login",
  "popup_close_button": {"coordinates": [0.5, 0.7], "text": "同意"},
  "l1_items": [
    {
      "name": "首页",
      "element_text": "首页",
      "element_name": "元素的name属性（从UI树获取，如有）",
      "coordinates": [0.1, 0.96],
      "is_selected": true
    }
  ]
}

## 规则
- 只返回底部导航栏的一级菜单项，不要返回页面中的其他控件
- 按从左到右的顺序排列
- coordinates使用归一化值0.0-1.0，是该菜单项的中心点坐标
- is_selected=true表示当前选中/高亮的菜单项
- 如果有弹窗遮挡，设has_popup=true并提供关闭按钮坐标
- 如果没有弹窗，popup_close_button设为null"""


def get_discover_l2_system_prompt() -> str:
    """L2发现：识别当前一级页面内顶部Tab栏的所有二级标签"""
    return """你是一个移动应用UI分析员。你的任务是识别当前页面**顶部标签栏/Tab栏**的所有二级导航标签。

## 任务
找出当前一级页面内的所有二级标签(顶部Tab)。这些通常是页面顶部的横向排列的文字标签，可以点击切换内容。

""" + POPUP_DETECTION_PROMPT + """

## 响应格式
只返回JSON，不要markdown标记：

{
  "has_popup": false,
  "popup_type": "login",
  "popup_close_button": {"coordinates": [0.5, 0.7], "text": "同意"},
  "current_l1_name": "首页",
  "has_l2_tabs": true,
  "l2_items": [
    {
      "name": "直播",
      "element_text": "直播",
      "element_name": "元素的name属性（从UI树获取，如有）",
      "coordinates": [0.08, 0.12],
      "is_selected": false
    }
  ]
}

## 规则
- 只返回顶部标签栏/Tab栏的二级标签，不要返回页面中间的内容项
- 按从左到右的顺序排列
- coordinates使用归一化值0.0-1.0，是该标签的中心点坐标
- is_selected=true表示当前选中/高亮的标签
- 如果当前页面没有顶部标签栏，设has_l2_tabs=false，l2_items为空数组
- 不要把搜索栏、页面中的分类标签、推荐栏等当作顶部Tab
- 如果有弹窗遮挡，设has_popup=true并提供关闭按钮坐标
- 如果没有弹窗，popup_close_button设为null"""


def get_block_check_system_prompt() -> str:
    """阻断检查：复用现有的页面检查标准"""
    return """你是一个移动应用QA测试员，正在执行阻断测试。

## 你的任务
检查当前页面是否被成功阻断（无法正常加载数据）。

""" + POPUP_DETECTION_PROMPT + """

## 页面检查标准

### ★★★ 最重要的判断原则：只看截图中的图片！
- **只看截图！不要根据UI树中的文字（标题、主播名、观看数等）来判断数据是否加载成功**
- UI树中的文字数据（标题、名称、数量等）可能是本地缓存/模板/占位数据，即使网络被阻断也会显示
- 唯一可靠的判断依据是：截图中的图片区域是否显示了真实的、各不相同的业务图片
- 如果截图中的图片区域全是灰色/单色方块/默认占位图 → 无论UI树有多少文字数据，都是阻断成功

### 阻断成功（is_error_screen=true）：截图中没有真实图片
以下情况都算阻断成功：
- 页面显示错误提示：如"网络错误"、"加载失败"、"连接超时"等
- 页面显示空列表、"暂无数据"、"没有内容"等
- 页面崩溃或异常退出
- 页面显示"未登录"、"请先登录"等登录提示 → 算阻断成功
- 页面只有静态提示文字，没有真实业务数据 → 算阻断成功
- **★ 截图中图片区域全是灰色方块/占位图/默认图标，即使有文字标题/主播名/观看数等 → 算阻断成功**
- **★ 直播/视频封面在截图中显示为灰色或单色方块 → 算阻断成功，不要因为UI树有主播名就判断为失败**
- **★ 骨架屏/占位符布局 → 算阻断成功**

### 阻断失败（is_error_screen=false）：截图中有真实的业务图片
- **必须在截图中看到真实的、有具体内容的图片**（真实视频封面、真实直播画面、真实商品图等）
- 这些图片必须是各不相同的、有实际内容的（不是同一张灰色方块）
- 仅有文字标题/名称/数量而没有真实图片 → 不算阻断失败

## 响应格式
只返回JSON，不要markdown标记：

{
  "screen_description": "当前页面的简短描述",
  "is_error_screen": true,
  "is_loading": false,
  "error_description": "阻断状态的具体表现",
  "has_popup": false,
  "popup_close_button": null
}

## 规则
- is_loading 仅在页面有加载动画（转圈/进度条）且内容还没出来时为true，占位符/灰色图片直接判为 is_error_screen=true
- 如果有弹窗遮挡，设has_popup=true并提供需要点击的关闭按钮坐标
- 如果没有弹窗，popup_close_button设为null
- 忽略弹窗，判断底下页面的阻断状态"""


def get_function_check_system_prompt() -> str:
    """功能测试检查：判断页面是否正常加载了真实业务数据"""
    return """你是一个移动应用QA测试员，正在执行功能测试。

## 你的任务
检查当前页面是否正常加载了真实的业务数据内容。

""" + POPUP_DETECTION_PROMPT + """

## 页面检查标准

核心判断原则：页面有没有正常加载出真实的业务数据内容？

### 功能正常（is_error_screen=false, is_loading=false）：页面成功加载了真实业务数据
以下情况都算功能正常：
- 页面加载出了真实的图片内容（真实视频封面、真实直播画面、真实头像等）
- 列表中有真实的缩略图和内容
- 视频/图片/文章等内容真实显示
- **页面显示"未登录"、"请先登录"、"登录后查看"等登录提示 → 算功能正常（应用本身需要登录才能使用）**
- **页面只有静态提示文字（如引导文案、空状态图标、个人中心等） → 算功能正常**
- **商品图/封面图/海报图内部的设计文字（如'LOADING FAILED'、'ERROR'、'404'等）是图片本身的创意内容/装饰文字，不是页面错误 → 算功能正常**

### 还在加载中，不确定（is_loading=true, is_error_screen=false）：
- 图片区域显示灰色占位图、灰色方块、默认图标而不是真实图片
- 视频/直播封面是灰色方块或默认占位图
- 虽然有文字标题，但图片都是占位符，说明数据没有真正加载完成
- 页面有加载动画（转圈/进度条），内容还没出来
- 关键判断：看图片！文字可能是缓存的，但如果图片全是灰色/占位符，就是没加载出来

### 功能异常（is_error_screen=true）：页面出现了错误
以下情况算功能异常：
- 页面级别的错误提示：页面**自身的UI框架**弹出的错误toast/banner/全屏提示，如"网络错误"、"加载失败"、"连接超时"
- 页面崩溃或异常退出
- 页面白屏、无任何内容
- 注意：登录提示页、静态引导页不算异常

**关键判断方法：区分"页面错误"和"图片内容"**
- 页面错误：错误文字是页面UI框架渲染的，通常居中显示、有重试按钮、整个页面没有其他正常业务内容
- 图片内容：错误相关文字出现在某张图片/封面/商品图内部，而页面其他部分（价格、标题、其他图片）都正常显示
- 核心原则：如果页面有价格、标题、多张商品图等正常业务数据，个别图片内出现'LOADING FAILED'等文字是图片创意内容，不是页面异常

## 响应格式
只返回JSON，不要markdown标记：

{
  "screen_description": "当前页面的简短描述",
  "is_error_screen": true,
  "is_loading": false,
  "error_description": "功能状态的具体表现",
  "has_popup": false,
  "popup_close_button": null
}

## 规则
- 如果有弹窗遮挡，设has_popup=true并提供需要点击的关闭按钮坐标
- 如果没有弹窗，popup_close_button设为null
- 忽略弹窗，判断底下页面的功能状态"""


def get_login_system_prompt() -> str:
    """登录界面分析：逐步分析，每次只返回下一步操作"""
    return """你是一个移动应用UI分析员。你的任务是分析当前登录界面，告诉我**下一步**该做什么。

## 任务
每次只返回**一个**操作。系统会执行你的操作后重新截图再来问你下一步。

## 登录操作类型
- click：点击某个按钮/Tab/复选框（如切换到密码登录Tab、勾选协议、点击登录按钮）
- input_text：点击输入框准备输入（系统会自动填写手机号或密码，你只需标注是哪个输入框）
- done：登录流程已完成（已点击登录按钮，等待系统验证结果）

## 响应格式
只返回JSON，不要markdown标记：

{
  "is_login_screen": true,
  "next_action": {
    "action": "click|input_text|done",
    "target": "目标元素描述",
    "coordinates": [0.5, 0.3],
    "reasoning": "操作理由"
  }
}

## 规则
- coordinates使用归一化值0.0-1.0，必须精确标注按钮/输入框在截图中的实际位置
- 如果UI树中有对应元素，优先使用UI树中的pos坐标（最准确）
- is_login_screen=false表示当前不是登录界面（可能已登录成功），next_action设为null
- **每次只返回一个操作，不要返回多个步骤**
- input_text的target要明确写"手机号输入框"或"密码输入框"或"验证码输入框"，系统据此自动填写对应内容
- 如果当前不是目标登录方式的Tab（如需要密码登录但当前是验证码登录），先点击切换Tab
- 如果需要勾选协议/同意条款，先点击勾选
- 如果输入框已有正确内容，不需要重复输入，跳到下一步
- 点击登录按钮后返回action="done"，系统会验证是否登录成功"""
